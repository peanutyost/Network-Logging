# Network Traffic DNS Logger - Configuration
# Copy this file to .env and customize for your environment

# ============================================================================
# Database Configuration
# ============================================================================
# Database type: postgresql or sqlite
DB_TYPE=postgresql

# PostgreSQL settings (used when DB_TYPE=postgresql)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=network_logging
DB_USER=postgres
DB_PASSWORD=postgres

# ============================================================================
# Packet Capture Configuration
# ============================================================================
# Comma-separated list of ports to monitor (leave empty to capture all traffic)
# Example: CAPTURE_PORTS=80,443,8080
CAPTURE_PORTS=

# Network interface to capture on (leave empty for default interface)
# Example: CAPTURE_INTERFACE=eth0
CAPTURE_INTERFACE=

# Optional BPF filter for advanced packet filtering
# Example: CAPTURE_BPF_FILTER=host 192.168.1.1
CAPTURE_BPF_FILTER=

# ============================================================================
# Application Configuration
# ============================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Number of days to look back when checking for DNS matches for orphaned IPs
ORPHANED_IP_DAYS=7

# Number of days to look back for retrospective threat detection scanning
THREAT_LOOKBACK_DAYS=30

# ============================================================================
# Authentication Configuration
# ============================================================================
# JWT Secret Key for authentication tokens (change in production!)
# Generate a secure random string, e.g.: openssl rand -hex 32
JWT_SECRET_KEY=your-secret-key-change-in-production

# ============================================================================
# Advanced Settings (optional)
# ============================================================================
# Capture snapshot length (bytes)
# CAPTURE_SNAPSHOT_LENGTH=65535

# Capture timeout (seconds)
# CAPTURE_TIMEOUT=1

